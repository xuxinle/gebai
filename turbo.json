{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@gebai/desktop#build": {
      "outputs": ["gebai", "gebai.exe"],
      "inputs": ["src/**", "package.json", "../../packages/server/src/sub-agents/**", "../../packages/server/scripts/build-subagents.ts"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
