# GEBAI 开发本地配置示例 —— 复制为 .env 并填入真实值（.env 不入版本库）
# 任何新增/变更的环境变量都必须同步写入本文件（含注释说明与示例值）

# LLM Provider（OpenAI 兼容接口，如 DeepSeek/通义/Kimi/GLM/Ollama）。
# 以下 GEBAI_LLM_* 均为「启动基准配置」：浏览器本地/会话/用户级可注入同名变量按任务覆盖（前端环境变量面板即可切换模型），未覆盖项沿用本配置
# 接口地址两种写法均可：服务根地址（自动追加接口路径，如 https://api.deepseek.com、https://open.bigmodel.cn/api/paas/v4）
# 或直接粘贴完整接口地址（如 https://open.bigmodel.cn/api/paas/v4/chat/completions——自动识别不重复拼接）
GEBAI_LLM_API_BASE=https://api.deepseek.com
# LLM 模型密钥（仅用于调用模型接口）
GEBAI_LLM_API_KEY=sk-your-key-here
GEBAI_LLM_MODEL=deepseek-chat
# 接口风格：openai（默认，chat/completions）| responses（OpenAI Responses API）| anthropic
# GEBAI_LLM_API_KIND=openai
# 单次上下文最大 token 预算（默认 128000）
# GEBAI_LLM_MAX_CONTEXT=128000
# 单次响应输出 token 上限：大文件生成截断防护（模型输出超限会被截断致工具参数非法；
# anthropic 接口必填，缺省 8192）；openai/responses 接口不配置时用服务端缺省
# GEBAI_LLM_MAX_OUTPUT_TOKENS=16384
# 主模型多模态能力声明：仅当主模型确实支持图片输入时设为 true（附件图片将 base64 内联进消息）；
# 默认 false——纯文本模型（如 deepseek-chat）配外挂视觉模型用下面的 GEBAI_VISION_* 即可
# GEBAI_LLM_MULTIMODAL=true
# 兼容 OpenAI 系旧变量名（GEBAI_* 优先）
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o
# 额外模型接口参数（JSON 对象，每次请求顶层合并进请求体；如推理强度/温度等，可被浏览器本地注入覆盖）
# GEBAI_LLM_EXTRA_PARAMS={"reasoning_effort":"high"}
# 命名模型路由表（JSON 对象，subsession_run 子会话运行的多路接口用）：
# {"路由名": {"model": "...", "api_base"?: "...", "api_key"?: "...", "api_kind"?: "openai|responses|anthropic", "max_context"?}}
# 子会话运行的 model 参数按路由名走独立端点/模型（未指定的项沿用主配置基准）——多分支多路并行分摊单路限流，
# 摆脱单轮串行的模型服务速度限制；未命中路由名视为字面模型名；非法 JSON 静默忽略；会话/任务级 env 同样生效
# GEBAI_LLM_ROUTES={"fast":{"model":"deepseek-chat","api_base":"https://api.deepseek.com"},"deep":{"model":"qwen-max","api_base":"https://dashscope.aliyuncs.com/compatible-mode/v1","api_key":"sk-xxx"}}

# 上下文压缩（自动触发：窗口剩余不足以支撑一次回复；目标水位 = 窗口 × 40%，压到目标即停）：
# 压缩目标水位比例（窗口的百分比，合法区间 0.1~0.9，默认 0.4）——调高则少丢历史（压得浅、压缩更频繁），
# 调低则压得更狠（一次丢弃更多历史，只由数百字摘要承载）；目标不高于触发线（比例定得比触发线还高时夹到触发线）
# GEBAI_COMPACT_TARGET_RATIO=0.4
# 近消息滑动窗口条数（默认 12，任意角色；上限为历史一半）：最近这么多条消息永不进压缩区间
# GEBAI_COMPACT_WINDOW=12
# 摘要请求是否走「与主循环逐字节同前缀」的原文前缀（命中服务端前缀缓存按缓存价计费）：
# 缺省自适应（按上次同模型调用是否命中缓存决定）、0/off 强制骨架行路径、1/on 强制原文前缀
# GEBAI_COMPACT_CACHE_PREFIX=1

# 额外多模态（视觉）模型：配置后启用独立视觉 Provider（vision 工具分析图片用），
# 接口地址/密钥/类型缺省时继承主模型配置；不配置则 vision 工具回落到主模型（须 GEBAI_LLM_MULTIMODAL=true）。
# 同样支持浏览器本地/会话/用户级同名变量按任务覆盖
# GEBAI_VISION_MODEL=qwen-vl-max
# GEBAI_VISION_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# GEBAI_VISION_API_KEY=sk-your-vision-key-here
# GEBAI_VISION_API_KIND=openai
# 视觉模型接口风格同主模型：openai | responses | anthropic
# GEBAI_VISION_MAX_CONTEXT=128000
# GEBAI_VISION_EXTRA_PARAMS={}

# 本地 CV（小模型图像识别，desktop 子Agent 的 desktop_ocr/desktop_locate/desktop_detect 用）：
# 非模型服务的本地 ONNX 推理（onnxruntime-web wasm 进程内运行，离线、不耗模型配额）。
# 资源获取：bun run resources:download（清单 scripts/resources.manifest.json——单文件多源
# modelscope/huggingface/镜像，.part 断点续传 + sha256 校验；--check 只校验现状）
# 模型目录解析顺序：GEBAI_CV_MODELS_DIR → 二进制形态释放的内嵌模型目录（~/.gebai/resources/models/cv/ocr/）→ {GEBAI_HOME}/resources/models/cv/ocr/（资源目录）
# GEBAI_CV_MODELS_DIR=/path/to/cv-models
# 输入图像降采样上限（像素，长边超过则等比缩小后识别；坐标仍映射回原始像素系；默认 1280）
# GEBAI_CV_MAX_SIDE=1280
# 目标检测（desktop_detect）自备 YOLO 模型与标签（每行一个类别）；模型不随构建内嵌。
# 缺省自动发现：{GEBAI_HOME}/resources/models/cv/detect/ 下唯一 .onnx 自动生效（资源目录 drop-in 即用，
# 多个时列出候选要求显式指定）——通常无需配置本变量；ultralytics 导出的 ONNX 自动读取
# 内嵌 imgsz/names 元数据，免标签文件与输入尺寸配置
# GEBAI_CV_DETECT_MODEL=/path/to/yolo-model.onnx
# GEBAI_CV_DETECT_LABELS=/path/to/labels.txt
# 检测输入边长覆盖（像素，缺省取模型元数据 imgsz、再缺省 640；范围 320-4096）
# GEBAI_CV_DETECT_SIZE=1280
# CV 推理后端（全局，检测与 OCR 共用）：auto（默认，GPU sidecar 可用即用、失败回落 wasm
# 并毒化不再重试）/ sidecar（强制 node 原生，失败不回落）/ wasm（强制进程内单线程 CPU）
# GEBAI_CV_BACKEND=auto
# 细分覆盖（优先于全局）：GEBAI_CV_DETECT_BACKEND / GEBAI_CV_OCR_BACKEND 取值同上
# GPU sidecar 执行提供者：auto（默认，Windows dml→cuda / macOS coreml→cuda 逐级探测，全失败
# 客卿 cpu 多线程兜底）/ dml / cuda / coreml / cpu（GEBAI_CV_DETECT_EP 为早期别名兼容）
# GEBAI_CV_EP=auto
# onnxruntime-node 解析目录（包根或其父目录；缺省按 {GEBAI_HOME}/resources/vendor/node_modules（
# 资源目录约定位置，npm 整包依赖闭包放入即生效，见 resources/README.md）→ {GEBAI_HOME}/vendor →
# node_modules 顺序解析；不可用时检测自动回落 wasm）
# GEBAI_CV_ORT_NODE_DIR=/path/to/onnxruntime-node

# 客卿（keqing agents，仅本地形态；任意语言接入见 keqing/README.md）：
# off/false/0 显式禁用（发现器不启动，vision/imgproc/disk 等边车子代理完全不可见）；
# 沙箱启用（服务端部署形态）时自动禁用——客卿仅限本地使用
# GEBAI_KEQING=off
# 内置 Python 子代理（vision）解释器解析优先项（目录含解释器；缺省语言目录 venv（keqing/python/venv）→ 系统 PATH）
# GEBAI_PYTHON_DIR=/path/to/python-dir

# 运行形态（默认本地模式）：local=本地模式（免登录，直接以 admin 用户工作）/ server=服务模式（账号密码登录）
# 兼容旧 GEBAI_AUTH（none→local、multi→server）；也可用 CLI 参数 --server 开启服务模式
# GEBAI_MODE=local
# 服务模式 admin 用户密码哈希（格式 salt:hash 均为 hex，与注册表 scrypt 加盐哈希一致）：
# 设置则启用 admin（每次启动覆盖其哈希），不设置则禁用 admin 用户。
# 生成命令：bun run --cwd packages/server hash-password -- '密码'（或管道/交互输入）
# GEBAI_ADMIN_PASSWORD_HASH=salt:hash
# 接口认证：REST 统一 Authorization: Bearer <token>（先 POST /api/v1/auth/login 获取令牌），WS 统一 auth.login。
# 不提供独立服务令牌（原 GEBAI_SERVICE_API_KEY 服务身份机制已移除）——避免任何服务端密钥进入 Agent 可达环境
# 注册审批模式：open=注册即用（默认）/ approval=注册待 admin 审批（用户管理页批准后启用，登录页提示等待审批）
# GEBAI_SIGNUP_MODE=open
# GEBAI_HOST=127.0.0.1
# GEBAI_PORT=3000
# （桌面端默认固定端口 47896——端口稳定才能保住浏览器本地数据，见 DESIGN「端口固定」；显式设置本变量可覆盖）
# playwright 子Agent 浏览器启动 channel：缺省 Windows 用系统自带 Edge（msedge，免下载浏览器），
# 其余平台不指定（默认 chromium，需 bunx playwright install chromium）；可选 msedge/chrome/chromium，留空 = 不指定
# GEBAI_PLAYWRIGHT_CHANNEL=msedge
# 透明浏览器代理（服务启动即安装，不依赖任何子Agent 的装载/裁剪；重启生效）：1/true 开启——
# 工具执行期间进程内发出的 http(s) fetch 自动改经浏览器会话（浏览器 UA 与会话登录态）发出，
# 供「内网站点仅限浏览器访问、进程直连被拒」场景；工具代码与模型零感知。
# 边界：LLM 请求恒直连；sh/py/js 子进程内网络不经代理；Request 对象输入与 FormData 等非字符串请求体回退直连；
# 单响应体上限 20MB；依赖 core/browser 桥接基建（需宿主机 node，浏览器 channel 同 GEBAI_PLAYWRIGHT_CHANNEL）
# GEBAI_BROWSER_PROXY=1
# GEBAI_LOG_LEVEL=info
# 外部身份扩展点（服务模式，同源部署集成网站）：外部身份兑换 GEBAI 令牌，免二次登录。
# 验证器二选一（互斥，同时设置启动报错）：
#   1) HMAC 共享密钥：网站用密钥对「用户名.过期时间戳」签名（HMAC-SHA256，hex），凭证格式 "{exp}.{sig}"，exp 为毫秒时间戳
#   GEBAI_EXTERNAL_AUTH_SECRET=change-me-shared-secret
#   2) HTTP 回调验证：GEBAI 把 {username, credential} POST 给回调 URL（5s 超时），业务系统自行校验（如查自己 localStorage 对应的服务端态），
#      返回 2xx + {"ok":true,"username":"可选覆盖"} 即通过；其余一律拒绝。
#      **回调必须 HTTPS**（localhost/127.0.0.1 例外，防中间人伪造 ok:true 响应），且**必须核验 username 与凭证归属一致**
#      （username 覆盖映射仅应在明确校验后使用）
#   GEBAI_EXTERNAL_AUTH_URL=https://your-site.example/api/gebai/verify
# 外部用户名不存在时自动创建 GEBAI 用户（普通角色，密码随机不可登录；false 时仅允许管理员预建的同名用户）
# GEBAI_EXTERNAL_AUTH_AUTOCREATE=true
# Web UI 同源直读宿主 localStorage 的凭证 key（值支持 {"username":"..","credential":".."} 或 "username:credential" 字符串；不设则仅支持 URL 参数注入）
# GEBAI_EXTERNAL_AUTH_STORAGE_KEY=
# 开发模式热刷新（bun run dev --reload 或本变量=1）：Web 源码变更自动重建（vite build --watch）并广播页面自动刷新
# GEBAI_DEV_RELOAD=1
# 数据目录（脚本调试=项目根目录，二进制默认 ~/.gebai/；二进制形态启动时自动加载该目录下的 .env——
# 桌面端把本文件复制到 ~/.gebai/.env 即可配置，真实环境变量优先）
# GEBAI_HOME=./data
# 允许的 CORS 来源，逗号分隔（默认 *）
# GEBAI_CORS_ORIGINS=*
# 部署在反向代理子路径（如 /gebai/）下无需任何配置：前端请求按页面 URL 相对解析、
# 代理剥离前缀转发即可（nginx：proxy_pass http://127.0.0.1:3000/; 见 DESIGN「反向代理支持」）
# 反向代理后面时设为 true，信任 X-Forwarded-For 等头
# GEBAI_TRUST_PROXY=false
# 路径沙箱：auto|on|off（服务端部署默认启用）；auth=none 默认用户豁免（操作者本人不受用户沙箱控制）
# GEBAI_SANDBOX=auto
# 脚本运行根隔离：auto|env|bwrap（**服务模式默认且强制开启**：off 与服务模式互斥，启动即拒绝，与 GEBAI_SANDBOX=off 同款防呆）。
# 开启后脚本的 HOME/TEMP/XDG 收敛到会话目录 {会话}/script-env/；Linux 检测到可用 bubblewrap 时升为文件系统隔离
# （系统目录只读、仅会话目录可写）；本地模式恒不收敛（操作者本人机器）
# GEBAI_SCRIPT_ISOLATION=auto
# 容器部署：容器内需允许 user namespace（`unshare --user --map-root-user echo ok` 输出 ok 即可用），
# 且容器内已装 bubblewrap；不可用时自动回落环境收敛并在日志给出原因
# 启动预载子 Agent 名单，逗号分隔：启动时注册其工具，每个新会话创建时自动装载
# （提示词 system 消息写入会话记录 + 工具注册，恢复历史会话时按会话记录自动还原）；为空 = 默认不预载任何子 Agent
# GEBAI_PRELOAD_SUB_AGENTS=
# 子 Agent 启停名单（逗号分隔，运行时生效——与构建期 GEBAI_BUILD_SUBAGENTS 裁剪互补，不改产物按部署收敛能力面）：
# GEBAI_SUB_AGENTS_ENABLE=code,explore  # 白名单：非空时仅保留名单内子 Agent（其余完全不可见——agent_list/
#                                       # agent_load/subsession_run/系统提示词注入均隐藏，热加载不复活）
# GEBAI_SUB_AGENTS_DISABLE=task,wps     # 黑名单：名单内子 Agent 移除（同上完全隐藏）；与白名单同时配置时
#                                       # 先白后黑（黑名单最终生效）；名单中的未知名启动告警忽略不阻断
# 构建期子 Agent 裁剪/预加载（仅 bun run build / desktop build 读取，运行时无效）：
# GEBAI_BUILD_SUBAGENTS=code,explore   # 仅打包指定子 Agent（逗号分隔；缺省 = 全部打包，按需产出精简二进制）
# GEBAI_BUILD_PRELOAD=code             # 构建时烘焙预加载（启动即装载；运行时 GEBAI_PRELOAD_SUB_AGENTS 仍可覆盖）
# GEBAI_BUILD_EXCLUDE_TOOLS=show,fetch_url
#                                      # 构建时排除全局工具（逗号分隔；被排除的工具不注册不暴露，subsession_run 内建
#                                      # 编排工具 flow/tool_schemas/js 与 vision 同规则；能力裁剪——实现仍打包）
# GEBAI_BUILD_EMBED_ENV=1              # 构建时把本文件中 GEBAI_LLM_*/GEBAI_VISION_* 模型配置烘焙为二进制
#                                      # 启动默认值（发行裁剪构建「开箱即用」用；运行时同名环境变量、
#                                      # {GEBAI_HOME}/.env 与前端配置仍可覆盖）。**密钥明文随二进制分发可被提取**——
#                                      # 仅限受信任小范围分发，建议低额度专用 Key；生成的 env-embedded 文件
#                                      # 编译后还原空态，密钥不入版本库
# 裁剪构建样例：bun run build:code（根 scripts/build-code-agent.ts，code 场景精简单文件二进制，
# 复制改三处清单即可产出其他场景产物）
# 两清单含未知名字时构建直接失败并列出可用名单。
# 构建期 CV 模型下载源（packages/server/scripts/build-cv-embed.ts 读取，运行时无效）：缺省 hf-mirror 的 RapidOCR
# 托管 PP-OCRv4 mobile（det/rec ONNX + 内嵌字典）；内网自备镜像/本地模型放
# {GEBAI_HOME}/resources/models/cv/ocr/（det.onnx/rec.onnx，已有文件跳过下载；也可用
# bun run resources:download 预先拉取）
# GEBAI_CV_MODEL_BASE=https://hf-mirror.com/SWHL/RapidOCR/resolve/main/PP-OCRv4
# 全局默认 UI 风格（acrylic|aether|cyberpunk|aurora|synthwave|matrix|tokyo-night|ink|cny|qinhan，默认 acrylic）
# GEBAI_UI_STYLE=acrylic
# 前端独立配置文件（可选，二次开发扩展点）：产物根 gebai.config.js——预置浏览器环境变量、把宿主
# localStorage 映射为歌白设置（主题/低功耗/文件展示/审批跳过等），并可关闭 URL 携带提示词自动运行；
# 模板与说明见 packages/web/public/gebai.config.js
# 仅启用指定工具 / 禁用指定工具（逗号分隔，格式 agent_tool 或全局工具名）
# GEBAI_TOOL_ENABLE=
# GEBAI_TOOL_DISABLE=
# 允许智能体修改自身代码（自我优化，默认关闭）
# GEBAI_SELF_MODIFY=false
# 安全模式：true 启用（仅启动时从 .env/环境变量加载，不可在会话/任务级修改）——
# 有风险的工具（sh/py/write/edit/delete_file/move_file/delete_tool/task_* 及子Agent 同名短工具）被阻止执行，
# 调用时直接返回限制信息给模型（仅只读操作可用）
# 安全模式：风险能力降级而非禁用（sh 只读命令白名单 + 重定向限用户目录；py/js 只读运行时——写/进程/网络屏蔽仅保留文件读取；
# write/edit/patch/file 限定用户目录内；task_* 调度类硬阻断；子Agent 工具按 Tool.safeMode 自主声明过滤注册）
# GEBAI_SAFE_MODE=false
# 预置项目注册表（JSON 数组 [{name,path,description}]，全局文件工具与 code 子Agent 独有工具用 project 参数传项目名；
# 也可写入会话/用户级环境变量；自由项目用 project 参数传项目根路径）
# CODE_PROJECTS=[{"name":"app","path":"/path/to/app","description":"主业务应用"}]
# 受限模式：true 时仅允许操作预配置项目（CODE_PROJECTS 清单或 CODE_PROJECT 绑定根），
# 文件工具必须携带 project 参数，自由路径（path）被拒绝；默认关闭（不限制）。（会话/用户级环境变量同样生效）
# CODE_RESTRICT_PROJECTS=false
# 文件工作台（/files 页面：目录树 + Monaco 查看/编辑 + Git 图形操作）总开关：false 时 /files 与 /api/v1/fs|git 全部 404
# GEBAI_FS_ENABLED=true
# 工作台写开关：false 时为纯只读检视环境（新建/改名/移动/复制/删除/上传/保存均拒绝；Git 写操作另由下方开关控制）
# GEBAI_FS_WRITE=true
# 服务模式下的额外白名单根（JSON 数组：字符串或 {name,path,description,writable}）；本地模式自动追加主目录/工作目录/盘符
# GEBAI_FS_ROOTS=[{"name":"日志","path":"/var/log","writable":false}]
# 读取/写入/上传/打包下载上限（字节，默认 10MB / 10MB / 100MB / 500MB）
# GEBAI_FS_MAX_READ=10485760
# GEBAI_FS_MAX_WRITE=10485760
# GEBAI_FS_MAX_UPLOAD=104857600
# GEBAI_FS_MAX_ZIP=524288000
# 目录树默认显示隐藏文件（默认 true，前端在「更多」菜单里可随时切换）
# GEBAI_FS_HIDDEN=true
# 工作台写操作审计（默认 true → {GEBAI_HOME}/audit-fs.jsonl：谁、哪个根、什么动作、成败）
# GEBAI_FS_AUDIT=true
# Git 写操作开关（暂存/提交/分支/合并/变基/重置/标签/暂存区/丢弃；默认 true；false 时仅保留只读查看与对比）
# GEBAI_GIT_WRITE=true
# Git 远程操作开关（fetch/pull/push/remote，需要网络与凭据；默认 true）
# GEBAI_GIT_REMOTE=true
# 终端面板开关（工作台内置持久 shell 会话：会话常驻服务进程、增量输出、可中断；默认 true）；
# 沙箱非豁免用户一律不开放（终端等同任意命令执行），只读环境（GEBAI_FS_WRITE=false）不允许执行命令
# GEBAI_TERMINAL=true
# 终端默认 Shell（id 或可执行路径，如 pwsh / powershell / cmd / bash / /bin/zsh）；留空按平台自动探测
# （Windows 依次 pwsh.exe → powershell.exe → cmd.exe，探测不到即回落下一个；POSIX bash/zsh/fish/sh/dash 与 $SHELL）
# GEBAI_TERMINAL_SHELL=pwsh
# 工作台语言服务器（LSP）：编辑器按文件语言探测 PATH 上的常驻服务器（gopls / rust-analyzer / clangd /
# pyright-langserver / pylsp / lua-language-server / yaml-language-server / bash-language-server …），
# 探到即提供补全、悬停、定义跳转、引用与诊断；**探不到不影响使用**（Monaco 内置语言服务与符号提取照旧）。
# 沙箱非豁免用户不开放（语言服务器等同拉起常驻子进程）；只读环境（GEBAI_FS_WRITE=false）仍可用（LSP 只读）
# GEBAI_LSP=true
# 语言服务器覆盖表（JSON 对象，键 = Monaco 语言 id；值 = 命令 / {command,args} / 候选数组 / null 关闭）。
# 显式列出即启用（含默认不启用的 typescript / json / css / html——那几种已由 Monaco 内置语言服务覆盖）
# GEBAI_LSP_SERVERS={"go":"C:/tools/gopls.exe","rust":{"command":"rust-analyzer","args":[]},"json":null}
# 空闲回收（毫秒，默认 600000）：无打开文档的语言服务器进程超时即退出
# GEBAI_LSP_IDLE_MS=600000
# 并发语言服务器进程上限（默认 4）
# GEBAI_LSP_MAX=4
# sh 工具（含 sh async 后台任务与 task 脚本命令）的命令解释器：Windows 留空自动解析——PATH 中的
# pwsh.exe（PowerShell 7+）优先、缺失回落系统内置 powershell.exe（Windows PowerShell 5.1）；
# 值 cmd 回落 cmd.exe（保留 && 与 %VAR% 语义）；POSIX 留空用 PATH 中的 bash（缺失回落 /bin/sh）；
# 两平台均可指定解释器名或绝对路径
# GEBAI_SH_SHELL=pwsh
# 服务模式 admin 密码哈希引导（见上方 GEBAI_ADMIN_PASSWORD_HASH 注释；已废止明文引导 GEBAI_BOOTSTRAP_ADMIN）
# 关闭数据生命周期 GC 清理任务（默认开启）
# GEBAI_GC_DISABLED=false
# 统一任务能力（定时/普通/闲时三类任务，共用一条调度队列）：默认启用（true，注册 task 子Agent——
# task_add/task_list/task_update/task_run/task_cancel/task_remove/task_files 工具——并启动调度器，
# REST /api/v1/tasks 同步可管）；任务为用户级资源 users/{user}/tasks.json（不随会话删除消失），
# 支持执行目标（独立新会话/专用会话/绑定会话）、时区、@at 一次性、错过补跑、运行历史与连续失败自动停用；
# 通知通道按任务配置（notify：webhook / 飞书群自定义机器人 / feishu_chat 群 chat_id——后者需下方飞书应用凭证）。
# 显式 false 关闭：task 子Agent、task_* 工具与 REST 管理面完全不可见
# GEBAI_TASKS_ENABLED=true
# 每用户同时运行的任务会话数（定时与普通任务并行上限；闲时任务另受「队列空闲 + 每用户仅 1 个」约束，
# 运行中的任务不因额度不足被中断——新任务排队等待）
# GEBAI_TASK_MAX_CONCURRENT=5
# 用户级待办（标题栏轮盘「待办」弹窗；待办为用户级资源 users/{user}/todos.json，与任务清单相互独立，
# 随时可手动执行——入队按序跑一次；开启 ⚡ 闲时自动执行的条目会绑定一个闲时任务，在队列空闲且没有运行中的
# 会话时按顺序自动执行，成功自动勾选完成、失败 3 次后停止自动执行）；显式 false 时 REST /api/v1/todos 返回 503
# GEBAI_IDLE_TODO_ENABLED=true
# 调度器门控（同一 GEBAI_HOME 下的跨进程互斥，默认 auto）：auto=主实例锁（{GEBAI_HOME}/.gebai-primary.json）
# 判定——同一数据目录下只有一个实例跑任务队列与待办闲时任务，其余退化为从实例（只提供 HTTP/WS），看门狗在主
# 实例退出/租约过期（5 分钟未续租）后自动接管；on=强制本实例跑调度（忽略锁，多实例同时 on 会重复调度）；
# off=完全不跑调度（只提供 HTTP/WS）
# GEBAI_SCHEDULER=auto
# 任务全局默认通知（任务未配置 notify 时自动使用；任务自配 notify 则不叠加）：
# 全局推送 webhook（http(s) 回调，POST JSON，SSRF 同规则校验）
# GEBAI_TASK_NOTIFY_WEBHOOK=https://hooks.example.com/task
# 全局飞书通知：群 chat_id（oc_ 前缀——应用身份推送，需下方飞书应用凭证）或群机器人 webhook URL
# GEBAI_TASK_NOTIFY_FEISHU=oc_xxxxxxxxxxxxxxxx
# 飞书云文档（feishu_docs 子Agent）全局应用凭证：子Agent 前缀 FEISHU_DOCS_APP_ID/FEISHU_DOCS_APP_SECRET 优先，
# 未配置时回落到全局 GEBAI_FEISHU_APP_ID / GEBAI_FEISHU_APP_SECRET
# GEBAI_FEISHU_APP_ID=cli_xxxxxxxxxxxx
# GEBAI_FEISHU_APP_SECRET=your-app-secret-here
# 云文档目标文件夹（可选，推荐）：用户飞书文件夹 URL 或 folder token——把本应用加为该文件夹协作者（可编辑）后，
# 机器人创建的文档/表格/多维表格/上传文件都落在该文件夹下、用户自动拥有全部权限；未配置则创建在应用云空间（归应用所有），
# 创建结果会附落位说明；权限类操作受限时工具也会引导配置本变量（子Agent 前缀 FEISHU_DOCS_FOLDER_URL 优先于本全局名）
# GEBAI_FEISHU_FOLDER_URL=https://xxx.feishu.cn/drive/folder/fldcnXXXXXXXX
# 飞书群基础能力（feishu_group 子Agent）专属凭证前缀：缺省回落上方全局 GEBAI_FEISHU_APP_ID/SECRET；
# 群列表/群成员查询（open_id+姓名——定时任务通知 @特定人的取材）/建群/拉人/移人/群内发消息
# FEISHU_GROUP_APP_ID=
# FEISHU_GROUP_APP_SECRET=
# 产品视频制作（reel 子Agent）：库根（默认 {GEBAI_HOME}/vendor/reel）——runtime/ 共享 Remotion 运行时
# （各视频工程以目录联接复用，依赖整机只装一次）、state/ 实测调优与渲染作业；创作能力（设计 token、镜头原语、
# 2.5D 相机、模板工程）内置在本包，无需任何外部载荷；Chrome 由 Remotion 下载到进程 cwd 的 node_modules/.remotion——
# 同实例各项目共用一份，位置与体积经 setup 报告；本机已有缓存即**直接继承**（版本不一致也照用并如实报出，
# 不让 Remotion 删缓存重下）；真无浏览器时才需要配置下方的浏览器可执行文件——离线/内网否则首次渲染会去联网下载
# REEL_LIBRARY_DIR=/path/to/reel
# 显式指定可复用的 Remotion 运行时目录（其内含 node_modules）：缺省会自动扫描 {GEBAI_HOME}/vendor/<其他库根>/runtime，
# 发现同版本即目录联接复用（省一次全量安装）；本子Agent 不依赖任何具体库根，无命中则自主安装
# REEL_SHARED_RUNTIME=/path/to/runtime
# 默认视频工程目录：未指定 project 参数的渲染与工程操作以它为基准（会话级环境变量同样生效）
# REEL_PROJECT=/path/to/video-project
# GPU 策略：auto（默认，探测到可用 GPU 即启用硬件编码（NVENC/VideoToolbox）与 Chrome GPU 光栅化）/ off（强制软件档）
# REEL_GPU=auto
# 浏览器可执行文件（Chrome/Chromium 路径）：指定后渲染不再查缓存也不联网下载（离线/内网必配）；
# 相对路径按视频工程目录解析，不存在则直接报错（不静默回落下载）；也可写入 .reel.json 的 browserExecutable
# GEBAI_REEL_CHROME_EXECUTABLE=/usr/bin/google-chrome
# 原生二进制目录（目录内需含 remotion/ffmpeg/ffprobe）：整体替换 Remotion 内置 compositor 与 ffmpeg，
# 用于换用带硬件编码器的 ffmpeg 构建；三件套缺一即报错；也可写入 .reel.json 的 binariesDirectory
# GEBAI_REEL_BINARIES_DIR=/opt/remotion-binaries
# 渲染准备阶段的两个子阶段时限（毫秒）：浏览器（含联网下载，默认 180000）与打包（默认 300000）；
# 超时即报出卡在哪一步、浏览器现状与修复动作（不再静默挂着）；慢网/首次下载、超大工程可放宽
# GEBAI_REEL_BROWSER_TIMEOUT_MS=180000
# GEBAI_REEL_BUNDLE_TIMEOUT_MS=300000
# 语音合成（tts 子Agent）：默认音色——tts_speak 未传 voice 参数时使用，可写完整名（Microsoft Huihui）或
# 关键词（慧慧 / Kangkang）；未匹配到会回落到本机中文女声并在结果里写明实际音色，tts_voices 可列出全部音色。
# 合成全程离线：引擎与音色都取自 Windows 系统内置语音（WinRT OneCore 优先、SAPI5 回退），不联网、不耗配额
# TTS_VOICE=慧慧
# 默认语音引擎：auto（默认）/ winrt / sapi——非 Windows 平台没有等价的内置离线引擎（工具如实报错，不做联网合成）
# TTS_ENGINE=auto
# 本地推理引擎（infer 子项目与 local_infer 子Agent）：**跨平台跨设备**——llama.cpp 引擎矩阵覆盖 Windows/Linux/macOS × CPU/CUDA/Vulkan/Metal/ROCm/SYCL，
# 有 GPU 走旗舰档（Qwen-AgentWorld-35B-A3B MoE），无 GPU 用 CPU 档 + 小模型（档位 cpu-small）也能跑通同一套接口；离线、零配额
# 子项目根目录：缺省 dev 模式自动推导为仓库根下的 infer/；二进制形态无该目录时须显式配置
# LOCAL_INFER_HOME=/path/to/gebai/infer
# 默认运行档位名（档位定义在 {LOCAL_INFER_HOME}/config/profiles.json）：fast=小量化全专家驻显存（实测解码 143 t/s）；
# balanced=略大且质量更高（138 t/s，显存仅余约 565 MB）；quality=大量化部分专家留 CPU（质量优先，82 t/s）；
# long-context=128K 上下文（93 t/s）；cpu-small=无 GPU/跨平台通用档（小模型 + CPU，如 Qwen2.5-0.5B）；
# 也可由 local_infer_start 的 profile 参数按次指定
# LOCAL_INFER_PROFILE=fast
# 服务端口（OpenAI 兼容端点）：接入 GEBAI 模型配置或命名路由时指向它
# LOCAL_INFER_PORT=8080
# 模型权重目录：缺省 {GEBAI_HOME}/resources/models/infer
# LOCAL_INFER_MODELS_DIR=/path/to/models/infer
# 单条推理请求超时（毫秒，local_infer_generate / local_infer_batch 缺省值）：本地模型很慢，勿设过小
# LOCAL_INFER_TIMEOUT_MS=600000
# 单次 local_infer_batch 的条目处理上限（未配置 = 不限制，全部处理）：批量是同步执行、会长时间占住会话，
# 建议按机时预算设一个上限自动分片（剩余条目用同一 job_id 续跑，也可按次传 max_items 参数覆盖）
# LOCAL_INFER_BATCH_MAX_ITEMS=200
# 启动方式：launcher（缺省，TS 层跨平台直启，不需要 PowerShell）/ script（Windows 上走 infer/scripts/run-server.ps1）
# LOCAL_INFER_LAUNCH=launcher
# 命名推理目标（JSON 数组）：把任务派到**局域网另一台机器或云端 OpenAI 兼容端点**，
# generate/batch 的 target 参数填 name 即可（不配则只能用 target="http://host:port" 直连）
# LOCAL_INFER_TARGETS=[{"name":"lan-4090","base_url":"http://192.168.1.9:8080","model":"qwen3-aw"},{"name":"cloud-gw","base_url":"https://llm.example.com/v1","api_key":"sk-xxx"}]
# 远端端点缺省 API Key（target 里未单独配 key 时使用；输出与产物里一律只出现掩码）
# LOCAL_INFER_REMOTE_API_KEY=sk-xxx
# **内网/离线编译**：额外的源码发现根（路径分隔符分隔）——预编译包需外网，内网可把 llama.cpp 源码目录或归档随包带入，
# 由 local_infer_sources 发现、local_infer_source_build 就地编译成引擎（缺省已包含 {GEBAI_HOME}/resources/src 与 resources/engines、以及 infer/engine）
# 联网机拉源码归档：bun run resources:download --only="src/**"（清单已登记 src/llama.cpp-<tag>.tar.gz）
# LOCAL_INFER_SOURCE_DIRS=/opt/src:/mnt/share/llama.cpp
# 把本地引擎接成一路模型算力：先在 local_infer 子Agent 里 local_infer_start（或直接跑 infer/scripts/run-server.ps1）拉起服务，
# 再把这一项并入上方 GEBAI_LLM_ROUTES 的同一个 JSON 对象——模型名取服务别名（档位脚本缺省 agentworld-35b-a3b），
# api_base 指向本地端点。适用离线/隐私场景与批量后台任务；该模型是推理型（思考过程在 reasoning_content）
# 且单卡吞吐有限，不宜当作低延迟的交互主模型。
# GEBAI_LLM_ROUTES={"local":{"model":"agentworld-35b-a3b","api_base":"http://127.0.0.1:8080/v1","api_kind":"openai"}}
# 该模型是推理型：思维链可占输出 90% 以上（实测同一工具调用任务，开思考 315 token / 关思考 27 token，
# 且工具调用结果完全一致）。经 EXTRA_PARAMS 关闭思考可显著提升响应速度（端到端 13.2s → 5.0s）；
# 确需复杂推理时去掉该项即可。注意 LLM 配置不支持思考开关的其他模型应保持本条缺省。
# GEBAI_LLM_EXTRA_PARAMS={"chat_template_kwargs":{"enable_thinking":false}}
# 一键起一个用本地引擎的独立实例（独立数据根、不抢调度与飞书长连接）：
#   pwsh -File infer/scripts/run-gebai-local.ps1        # 默认 :3100 → 本地 :8080；-EnableThinking 开思考
# 对外可访问地址（如 http://localhost:3000 或公网 https://example.com）：飞书用户授权（user_access_token）
# 自动回调用——auth_user_authorize 默认以 {GEBAI_PUBLIC_URL}/api/v1/oauth/feishu/callback 为回调地址，
# 需先在开发者后台「安全设置 → 重定向 URL」登记；未配置时回落到 http://localhost:{GEBAI_PORT 或 3000}
# GEBAI_PUBLIC_URL=http://localhost:3000
# 飞书机器人对话桥接：true 启用（长连接事件订阅，复用上方 GEBAI_FEISHU_APP_ID/SECRET 凭证），
# 用户可在飞书单聊/群聊中直接使用 Agent；会话映射 feishu_{chat_id}，服务模式按 open_id 自动创建映射用户
# GEBAI_FEISHU_BOT_ENABLED=false
# 飞书机器人行为配置（均默认 false 保持现状——仅最终回复 + 审批卡片）：
# 工具调用过程推送：true 后每个工具调用一条消息（🔧 开始 → ✔ 完成，原地更新，保留不撤回）
# GEBAI_FEISHU_BOT_NOTIFY_TOOLS=false
# 助手中间消息推送：true 后带工具调用的中间轮过程陈述/阶段结论发 markdown 卡片消息（与最终回复同构，保留不撤回）
# GEBAI_FEISHU_BOT_NOTIFY_ASSISTANT=false
# 自动审批工具：true 后需审批工具自动通过（审批卡片不再弹出）——部署方为飞书通道整体担责，
# 等价于为全部飞书会话统一开启审批跳过（含服务模式映射用户）；仅建议内网/低风险部署开启
# GEBAI_FEISHU_BOT_AUTO_APPROVE=false
# 飞书接口禁用 TLS 证书校验（true/1）：内网代理（HTTPS 中间人证书不受信任）场景使用，
# 对全部飞书出站请求生效——机器人桥接 REST/长连接 WebSocket、feishu_docs 子Agent 与 OAuth 回调兑换；
# 仅在可信内网环境开启，默认关闭
# GEBAI_FEISHU_INSECURE_TLS=false
# 允许 Webhook 推送私有网段目标（默认 false）
# GEBAI_WEBHOOK_ALLOW_PRIVATE=false
# 桌面端启动时不自动打开浏览器窗口（默认打开）
# GEBAI_NO_OPEN=1
# 服务端测试分片数（仅测试基础设施 scripts/test-parallel.ts 读取，运行时不生效）：
# 默认 min(8, max(2, CPU 核数))；也可用 `bun run --cwd packages/server test --shards=4` 覆盖
# GEBAI_TEST_SHARDS=8
