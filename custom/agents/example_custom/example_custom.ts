/**
 * 二开域示例占位：custom/agents/{name}/{name}.ts 导出 def 即自动注册（删除本目录不影响任何功能）。
 * 见 custom/README.md。
 */
export const def = {
  name: "example_custom",
  description: "custom/ 域自动扫描示例（放 def 文件即注册，随 custom/ 文件夹整体迁移）",
  systemPrompt: "你是 example_custom，custom/ 域自动扫描的示例子代理。",
}
