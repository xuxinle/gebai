根因总结（两处，均已实证）
🐛 BUG 1（主因）：keepAligning 240 帧循环的"拽底窗口"
sticky-scroll.ts 的 lockToBottom() 启动 keepAligning（KEEP_ALIGN_FRAMES=240，约 4 秒）：每帧 keepTick 执行 if (el.scrollTop < el.scrollHeight - el.clientHeight) scrollBottom()。

缺陷：keepTick 只在每帧开头检查一次 following，但 following 依赖 scroll 事件复位——如果用户滚轮上翻后 scrol 事件还没送达（rAF 先执行），keepTick 看到 following 仍为 true（旧值）→ scrollBottom() 拽回底部。用户在 lockToBottom()（发送消息/切会话/加载完成）后的 4 秒窗口内滚动，极易触发"滚一下跳一大段"。

🐛 BUG 2（会话加载特定场景）：loadMessages 尾部竞态
flushMsgBatch() 排期的 scrollIfSticky() rAF + lockToBottom() 后恢复 scrollTop=mem——rAF 回调执行把用户从历史位置拽到底部。

🐛 BUG 3（低性能模式，加剧因素）：contt-visibility
低性能模式下 .msg { content-visibility: auto; contain-intrinsic-size: auto 320px }——视口外消息以 320px 估算高度，滚动经过时高度突变到真实值（可能几千 px），视口内容被顶开，造成"跳一大段"。
