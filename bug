根因：index.ts:248 构造 AgentEngine 时漏传 cron，而 CronManager.attach() 只单向注入、不回填 → 引擎侧 opts.cron 恒为 undefined → cron_* 工具永远报“未启用”（与您的环境变量配置无关，配得没错）
修复（3 处小改动）：
engine.ts:360 新增 setCron() 方法
cron.ts:210 attach() 双向回填 engine.setCron(this)
DESIGN.md:1227 同步「定时任务·注入链路」设计说明


这个问题有意思——刚亲测过，我可以结合一手体感聊聊。总体判断：**方向是对的，“fork 而非 spawn”这个核心抉择很聪明，但它在一致性合入上有几个隐藏的坑，目前更像“80 分的好底子”。**

## 设计上的巧思

**1. 上下文快照 fork，解决了 agent_run 最痛的“重复交代背景”**
`agent_run` 派生新会话只回传一条 input，分支啥都不知道；`branch_run` 继承主线全部历史，发出去的第一句话就是任务本体。做“多方案对比”这种需要理解全局决策语境的事，这个差异是决定性的——不用把背景压缩复述一遍，天然低损耗。

**2. 自动合入 + 共享文件系统的组合拳**
报告免收割（不用 `bg_task wait` 那套），产物落盘立即可见（刚才 `cat` 读到 b2 的文件就是证明）。配合 `model` 参数还能做多路异构实验：同一问题让不同模型各答一版，本身就是个天然的评审局。

**3. 在编排谱系里站位清晰**
`flow`（确定性管道）→ `js`（动态逻辑）→ `agent_run`（隔离子任务）→ `branch_run`（共享背景并行）——“共享多少上下文 × 并不并行 × 怎么回收”三个维度各占一格，不重叠，心智负担低。

## 但有几个坑我得直说

**⚠️ 合入是文本追加，不是语义合并**
这是个 git 类比的**滑坡**：git merge 有三方合并树，squash/rebase 能收拾历史；这里的合入是把分支报告全文拼进主上下文。后果有两个——
- 分支**挂载不死则主线 context 线性膨胀**，八轮“不断分支合并”下来，主线会被分支报告淹没，后面真正要做决定的推理质量反而下降；
- 没有"摘要合入"或“只在需要时展开”的策略，膨胀不可逆。

**⚠️ 快照世界观 vs 实时文件系统的撕裂**
分支持有的是 fork 那一刻的**认知快照**，但它写的是**实时文件系统**。如果我等分支时顺手改了个文件，分支对此毫无感知，基于旧认知做的修改照样落盘——感知与现实的漂移没有任何检测提示。这在“主线静默等待”模式下没问题，在“边等边干活”模式下是个真雷。

**⚠️ 同文件并发写没有仲裁**
刚才测试刻意避开了这点：三个分支写三样互不相干的东西。但如果两分支都改 `config.go`，就是裸奔——要么后者整体覆盖前者，要么撞上 edit 的未读守卫报错。git 用 tree conflict 兜住的事，这里全靠 **prompt 自觉划地盘**（"b1 只许动 src/a.ts"）。文档说适合"多文件并行修改"，必须强调：**多*个*文件可以，同*一*个文件不行**。

**⚠️ 分支发出后不可补充输入**
只能 stop 不能插话。轻量化的必然代价，但碰到“跑到一半发现少给了一条关键约束”就只能砍掉重来。

## 如果继续迭代，我会提三条

1. **合入可选粒度**：报告合入前按需摘要/截断（哪怕只是框架层自动压成“结论+要点清单”，全文存档按需回放），保住主线预算——这条价值最高；
2. **worktree 选项**：允许每分支 fork 一个独立子目录工作区，返回“变更提案”（diff/patch 文本），由主线裁决后统一应用——把“争抢实时文件系统”变成“收提案再合库”，就把 git 类比从滑坡拉回来了，还能顺带解决同文件冲突；
3. **合入前的状态校验**：分支若涉及写操作，合入时报告“该文件自你 fork 后已被修改 N 次”之类的漂移警告。

## 一句话边界观

| 场景 | 该不该用它 |
|------|-----------|
| 多方案对比 / 异构模型评审 / 并行调研 | ✅ 本命场景 |
| 多个互不相干的文件并行改造 | ✅ 划清地盘即可 |
| 同一文件的协作修改 | ❌ 会打架，改走“分支产 patch → 主线合库” |
| 强顺序依赖任务链 | ❌ 这是 flow/js 的活 |
| 中间过程量大、只想要结论 | ❌ 上下文照付，用 agent_run 更省 |

总结：它的精髓不在“并行”（后台 `sh async` 早就能并行跑命令了），而在**带着完整认知去并行**——这让并行第一次能承担“做决策”而不只是“干体力活”。短板集中在“合回来的东西如何不打爆主线、不被旧认知污染”，底子好，值得在这个方向继续打磨。
