1、
歌白前端刷新页面无法恢复运行中会话——在途流式输出不再续接渲染、待决交互卡片（审批/提问/填值等）不重建，任务干等到超时，表现为会话“卡死”。

根因（一行话）：重构把恢复逻辑从 main.ts 拆到新文件 attach.ts 时，丢失了入口处的副作用导入。

故障链路
旧版 main.ts:750  setRunningAttach(attachRunningSession)   ← 注册钩子，功能正常
    ↓ 0be5ce6 重构拆分
新版 attach.ts:36  setRunningAttach(attachRunningSession)   ← 仍存在，但...

全仓库无任何 import "./attach"                             ← 死代码，模块永不加载
    ↓ 运行期表现
sessions.ts:275  void runningAttachHook?.()                ← 钩子恒为 null，可选调用静默跳过
    ↓ 后果
① 不调 client.attachSession() → 在途流不恢复，只显示已落盘历史
② 待决卡片不重建 → 事件已推送过一次且不重推，任务阻塞到超时
复制
排查中排除的嫌疑项：

服务端 session.attach handler（ws-handlers/session.ts:61）与旧版逐字等价 ✗
SDK 的 attachSession/attachStream 方法未变 ✗
SSE→WS 迁移发生在更早提交，本次只是删测试文件 ✗
快照 running 字段结构未变 ✗
该缺陷的隐蔽性：纯副作用模块没有具名导出，import 丢失后 typecheck/lint 全部绿灯通过；调用点用 ?.() 可选调用，运行期也不报错——是大型重构中最典型的静默回归。


2、工具名称和参数名称都改为蛇形，不要驼峰，大模型容易生成出问题，同时agent要兼容自适应模型的误差

3、确认flow工具必要性，上下文占用在，使用麻烦，如果必要性低直接去除，js工具可以完全覆盖

4、进入草稿页面要清除当前会话，刷新页面时还是空白页面
