{
  "name": "mathx",
  "description": "高性能数学计算子代理（C++ 边车常驻进程）：mathx_eval 数学表达式求值（变量代入/内置函数，适合反复高频计算）、mathx_eval_batch 批量求值、mathx_stats 数值统计（count/sum/mean/min/max/median/stdev）",
  "protocol": "1",
  "command": ["{agent_dir}/driver{exe}"],
  "build": {
    "windows": ["{lang_dir}/build.bat", "{agent_name}"],
    "unix": ["{lang_dir}/build.sh", "{agent_name}"]
  }
}
