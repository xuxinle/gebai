# 索引与检索设计

BM25 是经典词法检索排序函数：词频 tf 与逆文档频率 idf 的乘积经文档长度归一化。
参数 k1 控制词频饱和（常用 1.2~2.0），b 控制长度归一化强度（常用 0.75）。
中文检索无空格分词时，二元分词（bigram）是无词典依赖的稳健方案：
相邻两个字组成一个词元，查询与文档用同一切分方式即可对齐。

增量索引：以 mtime 与 size 判断文档是否变化，未变文档复用旧词条，
只对新增与修改的文件重新分词，重建成本与变更规模成正比。
