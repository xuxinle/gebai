{
  "name": "docqa",
  "description": "本地文档问答子代理（Python 边车常驻进程）：docqa_index 为目录建 BM25 索引（中文二元分词+英文词元，增量复用，索引持久化）、docqa_query 检索问答（命中片段高亮）、docqa_status 索引状态",
  "protocol": "2",
  "command": ["{python}", "-X", "utf8", "{lang_dir}/driver.py"]
}
