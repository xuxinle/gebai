{
  "name": "@gebai/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "server:build": "bun run scripts/gen-icon.ts && bun run --cwd ../web build && bun run ../server/scripts/build-web-bundle.ts && bun run ../server/scripts/build-subagents.ts && bun run ../server/scripts/build-tools.ts && bun run ../server/scripts/build-d2js.ts && bun run ../server/scripts/build-driver-embed.ts && bun run ../server/scripts/build-pwcore-embed.ts && bun build ./src/index.ts --compile --outfile=dist/gebai.exe --external @terrastruct/d2 && bun run scripts/embed-icon.ts",
    "launcher:build": "cargo build --release --manifest-path launcher/Cargo.toml && bun run scripts/copy-launcher.ts",
    "build": "bun run server:build && bun run launcher:build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@gebai/server": "workspace:*"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
